function [obstaclesInRange] = checkObstacles(obstacles, x0, N, v_max)

    predictionDist = N * v_max;
    obstaclesInRange = {};

    for i = 1:size(obstacles, 2)
        
        obsDistance = norm(x0 - obstacles(i).center) - obstacles(i).radius; 
        
        if predictionDist >= obsDistance
            
       
        
    end

end

